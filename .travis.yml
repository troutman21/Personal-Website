language: node_js
node_js:
    - "8"
script:
  -npm run test
before_deploy:
  npm run build:prod
deploy:
  provider: s3
  access_key_id: $AWS_ACCESS_KEY_ID
  secret_access_key: $AWS_SECRET_ACCESS_KEY
  bucket: $S3_BUCKET
  skip_cleanup: true
  acl: public_read
  local_dir: dist
